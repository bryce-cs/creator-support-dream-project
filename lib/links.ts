// External destinations shared across pages.

/**
 * Entry form. Linked from every "Apply" button (homepage hero + nav, and the
 * nav on /submissions and /terms) as well as "Submit Your Idea", so they all
 * land in the same place. Always opened in a new tab.
 */
export const TYPEFORM_URL = "https://form.typeform.com/to/MbmNRCNH";
