node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Runtime state, not source. Lives on the server volume.
# live-submissions.json holds submitter emails — never commit it.
data/overrides.json
data/live-submissions.json
