# Copy to .env.local for local dev, or set these on the server.
# .env.local is gitignored — never commit real values.

# ---- Typeform ----------------------------------------------------------------
# Personal Access Token: Typeform → Settings → Personal tokens.
# Without it the site falls back to data/submissions.json.
TYPEFORM_TOKEN=

# Only if you point the site at a different form.
# TYPEFORM_FORM_ID=MbmNRCNH

# Tag a response with any of these in the Typeform inbox and it drops off the
# public page. Comma-separated, case-insensitive. Defaults to "hide".
# TYPEFORM_HIDDEN_TAGS=hide,spam

# ---- Admin (/admin) ----------------------------------------------------------
# REQUIRED to turn /admin on. While unset, the page stays disabled rather than
# open. Use a long random string.
ADMIN_PASSWORD=

# Optional. Signs the admin session cookie; derived from ADMIN_PASSWORD if unset,
# which means changing the password logs everyone out. Set it explicitly to keep
# sessions alive across a password change.
# ADMIN_SESSION_SECRET=

# Where admin edits are written. Defaults to ./data/overrides.json — point this
# at your persistent volume if the working directory isn't persistent, or every
# edit will vanish on the next deploy.
# OVERRIDES_PATH=/data/overrides.json

# Where /live channel submissions are saved. Defaults to live-submissions.json
# in the SAME DIRECTORY as OVERRIDES_PATH, so pointing that at a volume covers
# both. Only set this to split them across different locations.
# LIVE_SUBMISSIONS_PATH=/data/live-submissions.json

# The Idea submissions shortlist. Same default: next to OVERRIDES_PATH.
# IDEA_SHORTLIST_PATH=/data/idea-shortlist.json

# Video summaries on the Idea submissions tab. Same default: next to OVERRIDES_PATH.
# IDEA_SUMMARIES_PATH=/data/idea-summaries.json

# ---- Kit / ConvertKit (/live workshop signup) ---------------------------------
# The signup form on /live posts to /api/subscribe, which forwards to Kit
# server-side. With none of these set the form answers "Signups aren't open yet"
# and the page still renders.
#
# v4 (preferred). Key: Kit -> Settings -> Advanced -> API -> API Key.
# Form ID: the number in the form editor URL, app.kit.com/forms/designers/<id>/edit
KIT_API_KEY=
KIT_FORM_ID=

# Kit custom fields the channel link and the "biggest challenge" answer are
# written to. Create the fields in Kit first (Subscribers -> custom fields) or
# the values are dropped silently. Default to "channel_url" and "yt_problem".
# Kit's key does not have to match the label its form builder shows, so confirm
# with GET /api/admin/kit rather than guessing from the label.
# KIT_CHANNEL_FIELD=channel_url
# KIT_PROBLEM_FIELD=yt_problem

# Legacy v3 fallback, used only if the pair above is unset.
# CONVERTKIT_API_KEY=
# CONVERTKIT_FORM_ID=

# ---- YouTube (/admin subscriber counts) ---------------------------------------
# YouTube Data API v3 key, used by the "Check subscriber counts" button on the
# Channel submissions tab. Google Cloud console -> APIs & Services -> enable
# "YouTube Data API v3" -> Credentials -> API key. Without it the button says so
# and nothing else is affected.
YOUTUBE_API_KEY=

# ---- Transcripts (/admin idea summaries) -------------------------------------
# YouTube blocks caption requests from Railway's IPs, so transcripts come from
# the Funk Machine (YT downloader) server, which runs yt-dlp behind cookies and a
# proxy. FUNK_API_KEY must match that server's FUNK_API_KEY. With neither set,
# this server asks YouTube directly, which works locally but not on Railway.
# FUNK_URL=https://funk-machine-web-production.up.railway.app
FUNK_API_KEY=

# ---- Anthropic (/admin idea summaries) ----------------------------------------
# Writes the one-sentence summary on each idea submission from its video's
# captions. console.anthropic.com -> API keys. Without it the Generate buttons
# say so, and summaries can still be typed by hand.
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001
